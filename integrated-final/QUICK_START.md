# 快速啟動指南

## 前置檢查

在啟動應用之前，請執行以下檢查：

```bash
# 1. 檢查圖片位置
npm run check:images

# 2. 安裝依賴（如果尚未安裝）
npm install
```

## 開發環境啟動

### 方法一：同時啟動前後端（推薦）

開啟兩個終端視窗：

**終端 1 - 後端服務器:**
```bash
cd integrated-final
npm run dev:server
```
應該看到：
```
✅ Server running on http://localhost:8080
📁 Serving from: .../integrated-final/dist
🌍 Environment: development
🔑 API Key configured: false
```

**終端 2 - 前端開發服務器:**
```bash
cd integrated-final
npm run dev
```
應該看到：
```
VITE v4.x.x ready in xxx ms
➜ Local: http://localhost:5173/
```

然後訪問：http://localhost:5173

### 方法二：僅啟動生產模式

```bash
cd integrated-final
npm run build    # 建置前端
npm start        # 啟動服務器
```

然後訪問：http://localhost:8080

## 連線狀態檢查

後端啟動後，在另一個終端執行：

```bash
npm run check:connection
```

應該看到：
```
✓ /api/health - 200 OK
✓ /api/status - 200 OK
✓ 後端服務正常運行
```

## 環境變數設定

### 生產環境
API 金鑰已設定在秘密變數中，無需額外配置。

### 本地開發
如果需要在本地測試 AI 功能，請：

1. 複製 `.env.example` 為 `.env`
2. 填入您的 Google Gemini API 金鑰：
```bash
GOOGLE_API_KEY=your_actual_api_key_here
PORT=8080
NODE_ENV=development
```

## 常見問題

### Q: 前端無法連接後端？
**A:** 檢查：
1. 後端是否已啟動？執行 `npm run dev:server`
2. Port 8080 是否被佔用？執行 `lsof -i :8080` 檢查
3. 執行 `npm run check:connection` 驗證連線

### Q: 圖片無法載入？
**A:** 檢查：
1. 執行 `npm run check:images` 驗證圖片位置
2. 開發環境確認 `/public` 目錄有圖片
3. 生產環境執行 `npm run build` 確保圖片複製到 `/dist`

### Q: API 回應 "API key not configured"？
**A:** 檢查：
1. 生產環境：確認秘密變數已設定
2. 本地開發：檢查 `.env` 檔案中的 `GOOGLE_API_KEY`
3. 重新啟動後端服務器

### Q: CORS 錯誤？
**A:** 
- 開發環境：Vite 代理應該自動處理
- 生產環境：同源部署，不應有 CORS 問題
- 檢查 `server.js` 中的 CORS 配置

## 測試 AI 功能

啟動應用後：

1. 選擇一個 AI 角色（智慧仙姑、桃花仙子等）
2. 輸入測試訊息
3. 檢查是否收到 AI 回應

如果 API 金鑰未設定，會顯示錯誤訊息。

## 部署到生產環境

```bash
# 1. 建置前端
npm run build

# 2. 確認環境變數
echo $GOOGLE_API_KEY

# 3. 啟動服務器
npm start
```

服務器會同時提供 API 和靜態檔案服務。

## 更多資訊

- 詳細配置說明：`README.md`
- 連線狀態報告：`CONNECTION_IMAGE_STATUS.md`
- 疑難排解：查看 `README.md` 的「故障排除建議」章節
